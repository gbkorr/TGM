


#sim = Sim() |> tick(1000)
#lin = Sim(Rules(link_range=0.12)) |> tick(2000)
#coh = Sim(Rules(cohesion=0.4)) |> tick(1000)
#lco = Sim(Rules(link_range=0.14, cohesion=0.4)) |> tick(2000)
#bran = Sim(Rules(branching=0.2)) |> tick(2500)
#bco = Sim(Rules(branching=0.2, cohesion=0.4)) |> tick(2000)
#wide = Sim(Rules(contraction=0.2)) |> tick(1000)

#long = Sim(Rules(mobility=4,cohesion=0.4,link_range=0.26)) |> tick(3000)


#winding branches
#Sim(Rules(branching=0.04),State(P_rules(size=8))) |> tick(10000) |> draw('bands',200)

