


#sim = Sim() |> tick(1000)
#sim4 = Sim(Rules(branching=0.2)) |> tick(3000)

#sim3 = Sim(Rules(cohesion=0.4)) |> tick(1000)

#sim2 = Sim(state=Init(P_rules(size=12))) |> tick(10000)

#Sim(Rules(branching=0.1)) |> tick(5000) |> draw()


#winding branches
#Sim(Rules(branching=0.02),State(P_rules(size=8))) |> tick(10000) |> draw('bands',200)

